/**
 * Test case for resolveBin.
 * Runs with mocha.
 */
'use strict'

const resolveBin = require('../lib/helpers/resolve_bin.js')
const assert = require('assert')
const co = require('co')

describe('resolve-bin', () => {
  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Resolve bin', () => co(function * () {

  }))
})

/* global describe, before, after, it */
